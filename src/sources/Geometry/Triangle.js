import Point from "./Point.js";

class Triangle {
  constructor(a, b, c) {
    this.a = a;
    this.b = b;
    this.c = c;

    this.neighbours = [];
  }

  isPointInside(point) {
    
  }
}

export default Triangle;